# Typecasting = the process of converting a variable from one data type to another
#               str(), int(), float(), bool()

name = "Tiago" # String
age = 25 # Int
gpa = 3.2 # Float
is_student = True # String

gpa = int(gpa) # Convert a float to a integer

print(gpa)

age = float(age)

print(age) # Convert a integer to a float

age = str(age) # Convert a integer to a string

print(age)

name = bool(name) # Convert a string to a boolean

print(name)